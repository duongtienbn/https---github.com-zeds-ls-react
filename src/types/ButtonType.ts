export interface ButtonType {
    styledButton: {
        color?: string,
        background?: string,
        fontWeight?: string,
        border?: string,
        position?: string,
        width?: string,
        maxWidth?: string,
        height?: string,
        padding?: string,
        margin?: string,
        hoverColor?: string,
        hoverBackground?: string,
        hoverOpactity?: string
    }
}